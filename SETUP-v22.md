# v2.2 적용 및 확인

## 추가된 기능
- 84A, 84B, 104A, 104B, 113A, 113B 타입 선택 탭
- 타입별 공급 세대수와 면적 정보
- 모바일 반응형 개념 평면도
- 타입별 상세 상담 버튼과 관심고객 팝업 연결

## 적용
1. 기존 프로젝트를 백업합니다.
2. 이 압축파일의 내용을 기존 프로젝트 폴더에 덮어씁니다.
3. 실행 중인 서버를 `Ctrl + C`로 종료합니다.
4. `.next` 폴더를 삭제합니다.
5. `npm install` 후 `npm run dev`를 실행합니다.
6. 브라우저에서 `Ctrl + Shift + R`로 강력 새로고침합니다.

## 확인
- 프리미엄 섹션 아래에 `6가지 주거 타입`이 표시됩니다.
- 탭을 누르면 면적과 공간 설명이 변경됩니다.
- `타입 상세 상담`을 누르면 관심고객 등록 팝업이 표시됩니다.

※ 개념 평면도는 실제 평면도가 아니며, 공식 평면 이미지는 추후 원본 JPG/PNG를 확보한 뒤 교체하는 것을 권장합니다.
