# v2.1 확인 순서

1. 기존 프로젝트 폴더를 백업합니다.
2. 압축을 풀고 전체 파일을 기존 프로젝트에 덮어씁니다.
3. 실행 중인 서버를 `Ctrl + C`로 종료합니다.
4. `.next` 폴더를 삭제합니다.
5. 아래 명령을 실행합니다.

```bash
npm install
npm run dev
```

6. 브라우저에서 `Ctrl + Shift + R`로 강력 새로고침합니다.
7. Hero의 `관심고객 등록` 버튼을 눌러 팝업이 뜨는지 확인합니다.
8. 테스트 등록 후 VS Code 터미널에 `[LEAD:TEST_MODE]`가 표시되는지 확인합니다.
