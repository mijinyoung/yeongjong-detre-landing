# 영종 디에트르 `exio.kr` 도메인 연결 안내

도메인을 결제한 것만으로 홈페이지 연결이 완료되지는 않습니다. Vercel에 도메인을 추가하고, 도메인 구입처의 DNS 값을 연결한 뒤, 홈페이지 대표주소를 변경해야 합니다.

## 1. Vercel에 도메인 추가

1. Vercel에서 영종 디에트르 프로젝트를 엽니다.
2. `Settings → Domains`로 이동합니다.
3. `exio.kr`과 `www.exio.kr`을 모두 추가합니다.
4. `exio.kr`을 광고에서 사용할 `Primary` 주소로 지정합니다.
5. `www.exio.kr`은 `exio.kr`로 이동하도록 Redirect를 설정합니다.

이 프로젝트의 대표주소는 짧고 기억하기 쉬운 `https://exio.kr`로 고정합니다. 광고 주소에도 `www`가 없는 주소만 사용합니다.

## 2. 도메인 구입처에서 DNS 연결

Vercel의 Domains 화면에 표시되는 DNS 값을 도메인 구입처의 DNS 관리 화면에 그대로 입력합니다. 도메인 업체와 연결 형태에 따라 필요한 레코드가 달라질 수 있으므로 임의의 값을 입력하지 말고 Vercel 화면의 안내값을 사용합니다.

연결 후 Vercel에 `Valid Configuration`이 표시될 때까지 기다립니다. HTTPS 인증서는 연결이 확인되면 Vercel에서 자동 발급합니다.

## 3. Vercel 환경변수 변경

Production 환경에서 아래 두 값을 대표 도메인으로 맞춥니다.

```text
NEXT_PUBLIC_SITE_URL=https://exio.kr
ADMIN_ALLOWED_ORIGINS=https://exio.kr
```

여러 관리자 접속 주소를 허용해야 한다면 쉼표로 구분할 수 있지만, 운영 시에는 대표 도메인 하나만 사용하는 것을 권장합니다.

환경변수를 변경한 뒤 반드시 새로 배포해야 검색 대표주소, 공유 주소, 사이트맵, 광고 전환 주소에 반영됩니다.

## 4. 연결 완료 확인

구매한 도메인으로 직접 접속한 뒤 다음 주소를 확인합니다.

```text
https://exio.kr/system-check
```

운영 점검 비밀번호를 입력하고 아래 항목이 모두 완료인지 확인합니다.

- 자체 도메인
- 도메인·HTTPS
- 운영 대표주소
- Google Sheets
- 문자 알림
- 관리자 보안
- 실제 문의 접수 모드
- 사용하는 광고 추적 도구

이후 홈페이지에서 본인 번호로 관심고객 등록을 한 번 실행해 시트 저장, 문자 수신, 관리자 화면 표시까지 확인합니다.

## 5. 광고 관리자에서 바꿀 주소

도메인 연결이 끝난 뒤에만 Meta와 Google 광고의 최종 도착 URL을 `https://exio.kr`로 변경합니다. 기존 `vercel.app`, `www.exio.kr`, `exio.kr` 주소를 광고에서 섞어 사용하지 않습니다.

광고 주소에는 관리자 화면에서 만든 UTM 값을 붙여 매체·캠페인·소재별 성과가 구분되도록 합니다.
